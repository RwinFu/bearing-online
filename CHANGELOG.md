# Changelog

## Unreleased — پنل عملیات کاربردی با ویرایش کامل

### نمای کلی و ناوبری
- تب جدید «نمای کلی» به‌عنوان صفحه پیش‌فرض پنل: ۸ شاخص (تعداد کالا، قابل خرید آنلاین،
  سفارش باز، استعلام در انتظار، لید جدید، شکایت جدید، ارزش موجودی، تامین‌کنندگان) با
  کلیک برای پرش به تب مربوطه + هشدار موجودی (ناموجود / رو به اتمام) + ۵ فعالیت اخیر.
- بج شمارنده روی تب‌های لید، استعلام، شکایت و سفارش برای کارهای باز.
- هر تب هنگام باز شدن رفرش می‌شود تا همیشه داده تازه ببینید؛ اسکرول لیست‌ها هنگام
  ویرایش حفظ می‌شود.

### ویرایش کالا و انبار (تب انبار)
- ویرایش قیمت دلاری هر کالا (فقط مدیر) با محاسبه خودکار قیمت تومانی؛ ویرایش زمان
  تحویل (روز)؛ موجودی، رزرو و منبع تامین مثل قبل (مدیر/انباردار).
- افزودن کالای جدید به کاتالوگ (فرم: برند، کد، نوع، ابعاد، قیمت، موجودی، کشور) و
  حذف کالا (فقط مدیر)؛ کالای جدید بلافاصله قابل جستجوست و همه‌چیز در localStorage
  می‌ماند (کالاهای سفارشی، حذف‌شده‌ها و قیمت‌های ویرایش‌شده).

### قیمت‌گذاری
- پیش‌نمایش زنده اثر نرخ/حاشیه سود روی یک کالای نمونه قبل از ذخیره؛ پس از ذخیره،
  پنل باز می‌ماند تا کار ادامه پیدا کند و همه قیمت‌ها همه‌جا به‌روز می‌شوند.

### لیدها، استعلام‌ها، شکایات
- لید: جستجو، یادداشت فنی، دکمه تماس/واتس‌اپ، حذف (مدیر/مشاور فنی).
- استعلام: نمایش اقلام هر RFQ، وضعیت فارسی، یادداشت داخلی، حذف (مدیر/مشاور استعلام).
- شکایت: پاسخ/یادداشت داخلی که برای مشتری هم در صفحه سفارش‌ها نمایش داده می‌شود + حذف.
- سفارش: جزئیات بازشونده (اقلام، مبالغ، تاریخچه وضعیت) + ویرایش کد رهگیری، شرکت حمل،
  موبایل/شهر/آدرس گیرنده و یادداشت داخلی (فقط مدیر).
- گزارش فعالیت: فیلتر متنی + پاک‌سازی (فقط مدیر).
- ورودی‌های کاربر در رندرهای پنل escape می‌شوند (رفع XSS از متن لید/شکایت/تامین‌کننده).

### تست
- `npm run test:ops` اضافه شد: ۳۳ بررسی اجرایی قابلیت‌های جدید پنل (ویرایش،
  افزودن/حذف، دسترسی نقش‌ها، ماندگاری، escape) — همه موفق.
- `static-check` و ۴۷ تست اجرایی jsdom قبلی همچنان موفق، بدون خطای JS.

## Unreleased — مثلث وکتور سراسری صفحه اول + کاتالوگ برندها

### افکت وکتور صفحه اول
- مثلث ساختاری (construction triangle) لایه وکتور صفحه اول حالا همیشه روشن است و در کل
  صفحه اول می‌چرخد: روی متن زیر موس می‌نشیند و در حالت بیکار (یا لمسی بدون هاور) سوار بر
  موج محیطی روی نزدیک‌ترین بلوک متنی مرئی حرکت می‌کند؛ دیگر فقط هنگام هاور دیده نمی‌شود.
- مسیر جاروی محیطی (idle sweep) عمودی‌تر شد تا برق و مثلث همه سکشن‌های صفحه اول را
  پوشش دهند، نه فقط نوار میانی.

### کاتالوگ شرکت برای هر برند
- مپ `BRAND_CATALOGS` در `07-brand-carousel.js`: یک کاتالوگ رسمی برای هر ۹ برند
  (لینک‌های رسمی SKF، Schaeffler medias، NTN، ZWZ، HKT، Timken، ASAHI، Flender و Ringspann).
- بخش «برندهای معتبر» صفحه اول: دکمه «کاتالوگ» کنار «مشاهده محصولات» در پنل معرفی برند؛
  با چرخش رینگ سه‌بعدی، لینک کاتالوگ برند فعال به‌روز می‌شود.
- صفحه برندها: دکمه «کاتالوگ» در فوتر هر کارت (باز شدن در تب جدید، بدون تداخل با کلیک
  کارت که محصولات را فیلتر می‌کند؛ پشتیبانی از کیبورد و هر دو زبان).
- برای میزبانی محلی کاتالوگ‌ها در آینده کافی است مقادیر `BRAND_CATALOGS` به مسیر فایل
  (مثلاً `assets/catalogs/skf.pdf`) تغییر کند؛ بدون نیاز به دست‌کاری مارک‌آپ.

### تست
- `static-check` و ۴۷ تست اجرایی jsdom: موفق، بدون خطای JS.

## 2026-09-21 — اصلاح جستجو، واکنش‌گرایی و نمایشگر سه‌بعدی

### جستجو
- رفع خالی‌ماندن اولین ورود به محصولات به‌علت کامنت placeholder در کانتینر نتایج.
- یکسان‌سازی ارقام فارسی/عربی، جستجوی کد و برند مستقل از ترتیب، نام‌های فارسی برندها،
  کدهای فاصله‌دار و ترکیب پسوند آب‌بند و کلیرنس؛ جلوگیری از نتایج عددی نامرتبط.
- افزودن جستجو در خود صفحهٔ نتایج، انتخاب پیشنهاد با کیبورد و لغو صحیح debounce.
- بازنشانی فیلترهای قبلی برای جستجوی جدید، نمایش دسته و ابعاد به‌صورت فیلتر قابل حذف،
  افزودن برندهای جاافتادهٔ کاتالوگ و اعتبارسنجی ابعاد مثبت/بازه‌ها.
- نگه‌داشتن جستجو، فیلترها، ابعاد، ترتیب و نوع نمایش در hash URL برای reload و Back.
- sort دیگر آرایهٔ اصلی دیتابیس را تغییر نمی‌دهد؛ پیشنهادها هم metadata نتایج را تغییر نمی‌دهند.
- متن جستجوی بی‌نتیجه escape می‌شود؛ مودال درخواست تامین دیگر خودکار باز نمی‌شود.

### موبایل و دسکتاپ
- حذف overrideهای فشرده و متناقض؛ خوانایی و اندازهٔ لمس مناسب‌تر، کارت تک‌ستونه در گوشی کوچک.
- اصلاح جای دکمهٔ جستجو، نمایش کامل لوگوی فارسی، لوگوی محلی بدون تعویض تصویر از اینترنت.
- منوی تبلت/موبایل تا 1023px، مدیریت فوکوس و Escape، فیلتر جمع‌شونده و جدول با اسکرول داخلی.
- قابل‌اسکرول شدن فهرست برندها؛ حذف بریده‌شدن پایین گروه‌های فیلتر.
- حذف میان‌برهای شناور از مسیر خرید و نمایشگر موبایل؛ حفظ دسترسی از کنترل‌های درون صفحه.
- رعایت reduced motion، نشانگر استاندارد سیستم و جلوگیری از ثبت تکراری رویداد tilt.

### درباره ما / Three.js
- رینگ‌ها با نسبت ابعادی متناسب‌تر، نور استودیویی و نمای داخلی آشکار، قفسه و ساچمه‌ها.
- سه حالت ساختار داخلی، مونتاژ و انفجاری، اسلایدر درصدی، زوم، بازنشانی و کیبورد.
- جداسازی ناحیهٔ رندر از کنترل‌ها؛ تنظیم فاصلهٔ دوربین بر اساس نسبت صفحه و بازشدگی مدل.
- اسکرول طبیعی صفحه در نمایشگر، کاهش pixel ratio گوشی و توقف حلقهٔ رندر در حالت پنهان/خارج دید.
- fallback ثابت با پیام واقعی و کنترل‌های غیرفعال برای نبود WebGL یا از دست رفتن context.

### تست
- ۴۵ تست اجرایی jsdom + بررسی ساختاری: موفق.
- ۳۹ بررسی Chromium در پنج عرض گوشی/تبلت/دسکتاپ، WebGL واقعی و RTL/LTR: موفق.
- دادهٔ واقعی انبار، درگاه پرداخت و تست روی Safari/گوشی فیزیکی خارج از این تغییرات هستند.

## 1.2.0 — تکمیل ساختار، رفع باگ‌ها، اجرای کامل بدون CDN

نسخهٔ آپلودشدهٔ ۱.۱.۰ ناقص بود: فقط ۷ فایل CSS در ریشهٔ ریپو وجود داشت و
**هر ۱۷ فایل JS تعریف‌شده در `index.html` غایب بودند** (`assets/js/…` وجود نداشت)، پس سایت
اصلاً بالا نمی‌آمد (هیچ صفحه‌ای رندر نمی‌شد). همچنین `</head>` تکراری و چند ارجاع خارجی باقی مانده بود.

### ساختار (تکمیل شد)
- ۷ فایل CSS از ریشه به `assets/css/` منتقل شدند.
- بخش `<script>` نسخهٔ اصلی (۳۵۱۸ خط) به ۱۷ فایل مستقل در `assets/js/` شکسته شد؛
  تمام ۳۵۱۹ خط منبع پوشش داده شده است (صفر خط گم‌شده) و هر فایل با `node --check` سالم است.
- لوگوها به `assets/img/brands/` و `favicon.svg` به `assets/img/favicon.svg` منتقل شدند.
- نسخهٔ اصلی به `docs/original-single-file.html` منتقل شد (بدون تغییر، به‌عنوان بکاپ).
- `</head>` تکراری در `index.html` حذف شد.

### باگ‌های رفع‌شده (همه در تست اجرایی چک می‌شوند)
1. **ترتیب لود / Race condition** – بلوک `ProductDatabase.forEach` که بالای فایل بود و `normalizePartCodeDisplay`
   را صدا می‌زد، به تابع `hydrateProductDatabase()` تبدیل و از `17-init.js` صدا زده می‌شود.
2. **جستجوی برند** – پسوند فقط وقتی معتبر است که بعد از بخش عددی کد آمده باشد؛ `ZWZ` دیگر به‌عنوان
   شیلد (`Z`) تفسیر نمی‌شود. `NTN` / `Timken` / `ZWZ` نتیجه می‌دهند.
3. **جستجوی ابعادی متنی** – `25x52x15` و `25*52*15` مستقیماً روی `d/D/B` مچ می‌شوند (قبلاً ۰ نتیجه).
4. **کلیرنس** – `6205 C3` روی فیلد `clearance` محصول مچ می‌شود، نه روی سیل/پسوند.
5. **زبان صفحات داینامیک** – بعد از هر رندر (جزئیات محصول، سبد، مقایسه، علاقه‌مندی، صفحه برندها)
   `applyLanguage()` صدا زده می‌شود؛ دیگر «Cart Items» در حالت فارسی باقی نمی‌ماند.
6. **`showPage` با شناسهٔ ناشناخته** – به خانه fallback می‌کند و overlay ترنزیشن گیر نمی‌کند.
7. **لوگوهای برندها** – از CDNهای خارجی (Siemens، Squarespace، Facebook…) به فایل‌های لوکال
   `assets/img/brands/` منتقل شدند؛ لینک Ringspann هم به فروشگاه خارجی نمی‌رود.
8. **رینگ برندها** – برای برند بدون فایل لوگو، `src=""` تولید نمی‌شود (وُردمارک CSS جایگزین می‌شود).
9. **سال کپی‌رایت** – `<span id="footer-year">` به‌صورت داینامیک پر می‌شود.
10. **WebGL** – اگر ساخت رندرر Three.js شکست بخورد، خطا گرفته و `#bearingFallback` نمایش داده می‌شود
    (قبلاً کل اسکریپت آن صفحه می‌شکست).
11. **پیش‌فاکتور** – در صورت نبود اینترنت، لوگو به وکتور لوکال و QR به حالت مخفی برمی‌گردد.
12. **نوار شناور موبایل** روی دکمه‌های مودال (رفع‌شده در CSS با `:has()`).

### وابستگی‌های خارجی → لوکال
هر ۴ وابستگی CDN داخل `assets/vendor/` قرار گرفتند (Tailwind v4 browser 4.3.3، Three.js 0.128/r128،
Font Awesome Free 6.4.0، فونت‌های Vazirmatn/Inter از fontsource). حالا سایت **آفلاین و بدون CDN** کامل
بالا می‌آید — مهم برای شبکهٔ ایران که دسترسی به `cdnjs` / `jsdelivr` / `fonts.googleapis.com` اغلب کند یا مسدود است.
نسخه‌ها، لایسنس‌ها و هش فایل‌ها در `assets/vendor/README.md` ثبت شده است.

### لوگوی هدر/فوتر
لوگوی رستری از `bearingonline.ir` دیگر پیش‌فرض نیست: اول لوگوی وکتور لوکال (SVG داخل خود صفحه) رندر
می‌شود و اگر تصویر رستری از اینترنت لود شد، جایگزین می‌شود. بنابراین هدر هیچ‌وقت خالی/شکسته نیست.

### تست
- `tools/smoke-test` اضافه شد (اختیاری، بخشی از سایت نیست): `npm test` =
  چک ساختاری (۱۵۴ هندلر inline، ۱۴۰ جستجوی id، ۳۰ ارجاع محلی) + اجرای واقعی سایت در jsdom با **۳۰/۳۰ چک موفق**:
  boot هر ۱۷ ماژول، جستجو (کد/برند/ابعادی/کلیرنس/فارسی)، محصول، سبد، تسویه، پرداخت mock، پیش‌فاکتور،
  حساب کاربری، لید، شکایت، مقایسه/علاقه‌مندی، پنل عملیات + RBAC، تعویض زبان (رفت‌وبرگشت fa/en)،
  مسیریابی hash، فیلترها و صفر خطای JS.
- چیدمان/CSS در jsdom قابل تست نیست (موتور رندر ندارد)؛ از طریق سرور استاتیک سرو و همهٔ ۳۵ ارجاع محلی
  به‌صورت HTTP 200 بررسی شدند و خروجی Tailwind (تولید ~۲۹ کیلوبایت CSS به‌صورت runtime) تأیید شد.

---

## 1.1.0 — بازسازی ساختار + رفع باگ (بدون تغییر ظاهر و رفتار)

> این نسخه در ریپو ناقص آپلود شده بود (فایل‌های JS جا افتاده بودند). در ۱.۲.۰ تکمیل و راستی‌آزمایی شد.

### ساختار
- فایل تک‌HTML ۷۰۹۰ خطی به `index.html` + ۷ فایل CSS + ۱۷ فایل JS (به ترتیب لود) شکسته شد.
- نسخهٔ اصلی در `docs/original-single-file.html` نگه داشته شد.

### باگ‌های رفع‌شده
۱. ترتیب لود / Race condition → `hydrateProductDatabase()`
۲. جستجوی برند (`NTN`، `Timken`) و پسوند
۳. جستجوی ابعادی متنی `25x52x15`
۴. کلیرنس `6205 C3`
۵. زبان صفحات داینامیک
۶. موبایل: نوار شناور روی دکمه‌های مودال
۷. `showPage` با شناسهٔ ناشناخته
۸. لوگوهای برندها از CDN → لوکال
۹. Favicon خارجی → لوکال + `theme-color`
۱۰. سال کپی‌رایت داینامیک
