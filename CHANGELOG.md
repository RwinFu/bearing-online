# Changelog

## 1.1.0 — بازسازی ساختار + رفع باگ (بدون تغییر ظاهر و رفتار)

### ساختار
- فایل تک‌HTML ۷۰۹۰ خطی به `index.html` + ۷ فایل CSS + ۱۷ فایل JS (به ترتیب لود) شکسته شد.
- نسخهٔ اصلی در `docs/original-single-file.html` نگه داشته شد.

### باگ‌های رفع‌شده
1. **ترتیب لود / Race condition** – پردازش `ProductDatabase` قبل از تعریف `normalizePartCodeDisplay` اجرا می‌شد. حالا در `hydrateProductDatabase()` (فراخوانی از `17-init.js`) انجام می‌شود.
2. **جستجوی برند نتیجه نمی‌داد** – `NTN`، `Timken` و… چون به «N» یا «K» ختم می‌شدند به‌عنوان پسوند (shield / tapered bore) تفسیر می‌شدند → ۰ نتیجه. پسوند فقط بعد از بخش عددی کد پذیرفته می‌شود.
3. **جستجوی ابعادی متنی** – `25x52x15` / `25*52*15` صفر نتیجه می‌داد؛ حالا مستقیماً روی d/D/B مچ می‌شود.
4. **کلیرنس** – `6205 C3` روی سیل مچ می‌شد؛ حالا روی فیلد کلیرنس محصول مچ می‌شود.
5. **زبان صفحات داینامیک** – سبد خرید، مقایسه، علاقه‌مندی، جزئیات محصول و صفحه برندها بعد از رندر انگلیسی می‌ماندند («Cart Items», «Proceed to Checkout»). `applyLanguage()` بعد از هر رندر صدا زده می‌شود.
6. **موبایل: نوار شناور روی دکمه‌های مودال** – دکمه‌های «ارسال درخواست/واتس‌اپ» زیر نوار Quick-Action می‌رفتند. وقتی مودال/منو باز است نوار مخفی می‌شود.
7. **`showPage` با شناسهٔ ناشناخته** – TypeError می‌داد و overlay ترنزیشن گیر می‌کرد؛ حالا به خانه fallback می‌کند.
8. **لوگوهای برندها از CDNهای خارجی** (Siemens, Squarespace, Facebook…) که hotlink را می‌بستند → لوکال در `assets/img/brands/`. لینک Ringspann دیگر به فروشگاه خارجی نمی‌رود.
9. **Favicon خارجی** (bearingonline.ir با timeout) → `assets/img/favicon.svg` لوکال + `theme-color`.
10. **سال کپی‌رایت** ثابت ۲۰۲۴ → داینامیک.

### تست
- Playwright (دسکتاپ ۱۳۶۶px + موبایل ۳۹۰px): صفر خطای JS در همهٔ صفحات، جریان کامل جستجو → محصول → سبد → تسویه → پرداخت mock → پیش‌فاکتور → پنل سفارش‌ها → پنل عملیات، ثبت لید، تعویض زبان، بازیابی state بعد از reload.
