{
  "name": "bearing-online-smoke-test",
  "version": "1.0.0",
  "private": true,
  "description": "Optional dev-only smoke test for the static Bearing Online site (jsdom + static cross-checks). The site itself needs no build step.",
  "scripts": {
    "test": "node static-check.js && node run.js",
    "test:browser": "node browser-test.js",
    "test:ops": "node ops-edit-check.js"
  },
  "devDependencies": {
    "jsdom": "^30.1.0",
    "playwright": "^1.63.0"
  }
}
